name: HR News Crawler

on:
  schedule:
    - cron: "0 1 * * 1-5"  # 每个工作日，北京时间 09:00（UTC+8）
  workflow_dispatch: {}     # 支持手动运行

jobs:
  run:
    runs-on: ubuntu-latest

    env:
      # ---- 在这里填钉钉机器人 Webhook & Secret ----
      DINGTALK_WEBHOOK: "https://oapi.dingtalk.com/robot/send?access_token=00c49f5d9aab4b8c86d60ef9bc0a25d46d9669b1b1d94645671062c4b845dced"
      DINGTALK_SECRET: "SEC2431e95f7bca3b419185a0fbd80530829c45c94977ba338022400433f064c6ad"
      # ---------------------------------------------

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          pip install -r requirements.txt

      - name: Run HR News Crawler
        run: |
          python hr_news_crawler.py

